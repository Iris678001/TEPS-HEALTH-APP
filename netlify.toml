[build]
  command   = "npm run build"
  publish   = ".next"

[build.environment]
  NODE_VERSION = "20"
  SECRETS_SCAN_ENABLED = "false"
  SECRETS_SCAN_OMIT_KEYS = "DATABASE_URL,NEXT_PUBLIC_SUPABASE_URL,NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  # Extend timeout for DB-heavy operations (admin panel seeding, bulk exports)
  included_files = ["prisma/**", "node_modules/.prisma/**"]

[[headers]]
  for = "/api/*"
  [headers.values]
    Cache-Control = "no-store"
